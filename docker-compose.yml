services:
  dermatitis-tracker:
    build: .
    image: dermatitis-tracker:latest
    container_name: dermatitis-tracker
    ports:
      - "8080:80"
    environment:
      DB_PATH: /data/dermatitis-tracker.sqlite
      PHOTOS_DIR: /data/photos
      APP_PIN: ${APP_PIN:-}
    volumes:
      - dermatitis-tracker-data:/data
    restart: unless-stopped

volumes:
  dermatitis-tracker-data:
